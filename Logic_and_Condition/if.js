/*
     if statement: Use the if statement to specify a block of JavaScript code to be executed if a condition is true.
     Syntax:
        if (condition){
            // block of code to be executed if the condition is true.
        }
    Note: 
        *if is in lowercase letters.Uppercase letters(if or IF) will generate a JavaScript error.
        * if (condition*) => * this condition result always return (True or False) 
*/

var passing_result = 70;

if (passing_result >= 70){
    console.log('I have passed in the examination ')
}

var a = 30;
var b= 20;

if (a > b){
    console.log(`${a} is greeter than ${b}`)
}
